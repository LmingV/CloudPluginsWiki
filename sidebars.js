// @ts-check

/**
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  mainSidebar: [
    'intro',
  ],
  cosmeticsSidebar: ['cosmetics/index'],
  chatSidebar: ['chat/index'],
};

export default sidebars;
